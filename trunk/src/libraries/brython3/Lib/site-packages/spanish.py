usage = '''
Spanish equivalent of English instructions.
For example, we can use avanzar() [Spanish] instead
of move() [English].

To use on the English site: http://reeborg.ca/world.html


Robot:
------

en_objetivo = at_goal
en_orientacion_final = at_goal_orientation
construir_pared = build_wall
nada_al_frente = front_is_clear
nada_a_la_derecha = right_is_clear
tiene_ficha = has_token
mirando_al_norte = is_facing_north
avanzar = move
depositar = put
ficha_aqui = token_here
objeto_aqui = object_here
recoger = take
girar_a_la_izquierda = turn_left

class:
------
RobotUsado = UsedRobot

For example:

reeborg = RobotUsado()
reeborg.avanzar()    # or reeborg.move()

General instructions:
---------------------
escribir = write
termine = done
repetir = repeat
pensar = think
seleccionar_mundo = select_world
examine = inspect
ver_fuente = view_source
sonido = sound
pausa = pause

'''


escribir = write
termine = done
repetir = repeat
pensar = think
seleccionar_mundo = select_world
examine = inspect
ver_fuente = view_source
sonido = sound
pausa = pause

en_objetivo = at_goal
en_orientacion_final = at_goal_orientation
construir_pared = build_wall
nada_al_frente = front_is_clear
nada_a_la_derecha = right_is_clear
tiene_ficha = has_token
mirando_al_norte = is_facing_north
avanzar = move
depositar = put
ficha_aqui = token_here
objeto_aqui = object_here
recoger = take
girar_a_la_izquierda = turn_left

from __main__ import _UsedRobot

class RobotUsado(_UsedRobot):

    def en_objetivo(self):
        self.at_goal()

    def en_orientacion_final(self):
        self.at_goal_orientation()

    def construir_pared(self):
        self.build_wall()

    def nada_al_frente(self):
        self.front_is_clear()

    def nada_a_la_derecha(self):
        self.right_is_clear()

    def tiene_ficha(self):
        self.has_token()

    def mirando_al_norte(self):
        self.is_facing_north()

    def avanzar(self):
        self.move()

    def depositar(self, arg="token"):
        self.put(arg)

    def ficha_aqui(self):
        self.token_here()

    def objeto_aqui(self):
        self.object_here()

    def recoger(self, arg="token"):
        self.take(arg)

    def girar_a_la_izquierda(self):
        self.turn_left()


