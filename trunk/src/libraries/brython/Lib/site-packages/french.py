usage = '''
Équivalences françaises des commandes anglaises.  
Par exemple, on peut utiliser avance() [français]
comme synonyme de move() [anglais]

À utiliser sur le site anglais  http://reeborg.ca/world.html

À noter qu'il existe un site français: http://reeborg.ca/monde.html
pour lequel vous n'avez pas besoin d'utiliser ces équivalences

Robot:
------

au_but = at_goal
au_but_orientation = at_goal_orientation
construit_mur = build_wall
rien_devant = front_is_clear
rien_a_droite = right_is_clear
a_des_jetons = has_token
face_au_nord = is_facing_north
avance = move
depose = put
jeton_ici = token_here
objet_ici = object_here
prend = take
tourne_a_gauche = turn_left

class:
------
RobotUsage = UsedRobot

Par exemple:

reeborg = RobotUsage()
reeborg.avance()    # ou reeborg.move()

Commandes génériques:
---------------------
ecrit = write
termine = done
repete = repeat
pense = think
selectionne_monde = select_world
examine = inspect
voir_source = view_source
son = sound
''' 


ecrit = write
termine = done
repete = repeat
pense = think
selectionne_monde = select_world
examine = inspect
voir_source = view_source
son = sound

au_but = at_goal
au_but_orientation = at_goal_orientation
construit_mur = build_wall
rien_devant = front_is_clear
rien_a_droite = right_is_clear
a_des_jetons = has_token
face_au_nord = is_facing_north
avance = move
depose = put
jeton_ici = token_here
objet_ici = object_here
prend = take
tourne_a_gauche = turn_left

from __main__ import _UsedRobot

class RobotUsage(_UsedRobot):

    def au_but(self):
        self.at_goal()

    def au_but_orientation(self):
        self.at_goal_orientation()

    def construit_mur(self):
        self.build_wall()

    def rien_devant(self):
        self.front_is_clear()

    def rien_a_droite(self):
        self.right_is_clear()

    def a_des_jetons(self):
        self.has_token()

    def face_au_nord(self):
        self.is_facing_north()

    def avance(self):
        self.move()

    def depose(self, arg="token"):
        self.put(arg)

    def jeton_ici(self):
        self.token_here()

    def objet_ici(self):
        self.object_here()

    def prend(self, arg="token"):
        self.take(arg)

    def tourne_a_gauche(self):
        self.turn_left()
