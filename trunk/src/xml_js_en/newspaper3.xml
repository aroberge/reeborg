<nav>
<a class="fake_button blue-gradient" href="Javascript:void(0);" onClick="load_page('while')">Previous</a>
<a class="fake_button blue-gradient" href="Javascript:void(0);" onClick="load_page('toc')">Table of Contents</a>
<a class="fake_button blue-gradient" href="Javascript:void(0);" onClick="load_page('refine1')">Next</a>
</nav>
<h1>Newspaper delivery revisited again</h1>

<p>Let's go back to our newspaper delivery example.  Below is
    an outline of a solution that will work both Ms. Lovelace's
    world <span class="world">Newspaper&nbsp;1</span>,
    and Mr. Babbage's <span class="world">Newspaper&nbsp;2</span>.</p>


<pre class="jscode">
function climb_up_one_floor() {
    turn_left();
    move();
    turn_right();
    move();
    move();
}

function climb_down_one_floor() {
    move();
    move();
    turn_left();
    move();
    turn_right();
}

function get_money() {
    while ( ... ) {
      // single line of code
    }
}

// === End of definitions ===

take("star");
while ( !token_here() ){
    // single line of code
}
get_money();
put("star"); // leave paper
turn_around();
while ( ! ... ){
    // single line of code
}</pre>

<p>It is up to you to fill up the missing tests and other lines of code.
Once you are done, you might want to go back to
<a href="Javascript:void(0);" onClick="load_page('newspaper2')">Newspaper delivery revisited</a>
and see how much shorter this new version, which works in both worlds,
<small>as you have checked, ... right?</small>, is.</p>

<a class="left-link" href="Javascript:void(0);" onClick="load_page('while')">Previous</a>
<a class="right-link" href="Javascript:void(0);" onClick="load_page('refine1')">Next</a>