<nav>
<a class="fake_button blue-gradient" href="Javascript:void(0);" onClick="load_page('while')">Précédent</a>
<a class="fake_button blue-gradient" href="Javascript:void(0);" onClick="load_page('toc')">Table des matières</a>
<a class="fake_button blue-gradient" href="Javascript:void(0);" onClick="load_page('refine1')">Suivant</a>
</nav>
<h1>Une nouvelle livraison de journaux</h1>

<p>Retournons à l'exemple de la livraison de journaux.
    Ci-dessous est une esquisse de solution qui permettra à
    Reeborg de livrer le journal de Madame Lovelace,
    <span class="world">Journal&nbsp;1</span>, ainsi que
    celui de Monsieur Babbage, <span class="world">Journal&nbsp;2</span>,</p>


<pre class="jscode">
function monte_un_étage() {
    tourne_à_gauche();
    avance();
    tourne_à_droite();
    avance();
    avance();
}

function descend_un_étage() {
    avance();
    avance();
    tourne_à_gauche();
    avance();
    tourne_à_droite();
}

function prend_argent() {
    while ( ... ) {
      // une seule instruction
    }
}

// === Fin des définitions ===

prend("étoile");
while ( !jeton_ici() ){
    // une seule instruction
}
prend_argent();
dépose("étoile");
demi_tour();
while ( ! ... ){
    // une seule instruction
}</pre>

<p>C'est à vous de compléter le programme.  Lorsque vous aurez terminé et testé votre
    programme <b>avec les deux mondes</b>, comparez avec la solution de
    <a href="Javascript:void(0);" onClick="load_page('newspaper2')">Autre livraison de journaux</a>;
    vous verrez que cette nouvelle solution, qui fonctionne pour les deux mondes,
    est beaucoup plus courte que la précédente qui ne fonctionnait que pour un seul monde.</p>

<a class="left-link" href="Javascript:void(0);" onClick="load_page('while')">Précédent</a>
<a class="right-link" href="Javascript:void(0);" onClick="load_page('refine1')">Suivant</a>