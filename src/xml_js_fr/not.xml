<nav>
<a class="fake_button blue-gradient" href="Javascript:void(0);" onClick="load_page('hurdles3')">Précédent</a>
<a class="fake_button blue-gradient" href="Javascript:void(0);" onClick="load_page('toc')">Table des matières</a>
<a class="fake_button blue-gradient" href="Javascript:void(0);" onClick="load_page('while')">Suivant</a>
</nav>
<h1>Pas vrai!</h1>
<p>Reeborg est fâché.  Il ne pleut <b>pas</b>; il ne neige <b>pas</b>; mais Reeborg ne peut <b>pas</b> aller
jouer dehors et s’entraîner pour la course aux haies.  Il doit attendre que vous appreniez la négation
en Javascript.</p>
<h2>C'est le temps d'être négatif</h2>
<p>Javascript, comme plusieurs autres langages de programmation,
    utilise le symbole <code>!</code> pour représenter la négation.
    Au lieu d'apparaître à la fin d'une phrase, ce point d'exclamation apparaît avant un objet ayant
    un sens booléen (vrai ou faux) et en reverse le sens.  Ainsi <code>!true</code>
veut dire <code>false</code>, et <code>!false</code> signifie <code>true</code>.</p>
<h2>SVP, faites plaisir à Reeborg</h2>
<p>Vous avez déjà écrit un programme permettant à Reeborg de prendre part à une course
    aux haies.  Une partie de votre programme se lisait comme suit:</p>
<pre class="jscode">
function avance_et_saute_jusqu_au_but () {
    if ( au_but() ) {
        // instruction ...
    } else if ( rien_devant() ) {
        // instruction ...
    } else {
        // instruction ...
    }
}</pre>

<p>Vous pouvez rendre Reeborg heureux en écrivant trois versions différentes de ce
    programme démontrant différents choix d'utilisation du symbole de négation <code>!</code>
    ainsi que différentes combinaisons <code>if/else</code>.
</p>

<pre class="jscode">
// premier choix:

function avance_et_saute_jusqu_au_but () {
    if ( au_but() ){
        // instruction ...
    } else if ( !rien_devant() ){
        // instruction ...
    } else {
        // instruction ...
    }
}

// deuxième choix ... plus difficile

function avance_et_saute_jusqu_au_but () {
    if ( !au_but() ){
        if ( rien_devant() ){
            // instruction ...
        } else {
            // instruction ...
        }
    } else {
        // instruction ...
    }
}

// troisième choix


function avance_et_saute_jusqu_au_but () {
    if ( !au_but() ){
        if ( !rien_devant() ){
            // instruction ...
        } else {
            // instruction ...
        }
    } else {
        // instruction ...
    }
}
</pre>

<a class="left-link" href="Javascript:void(0);" onClick="load_page('hurdles3')">Précédent</a>
<a class="right-link" href="Javascript:void(0);" onClick="load_page('while')">Suivant</a>