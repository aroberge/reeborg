/**
 * @namespace UnitTest
 *
 * @desc Fake namespace so that we can easily document and find files where unit tests
 * are found for a given function.
 */

/**
 * @namespace IntegrationTests
 *
 * @desc Fake namespace so that we can easily document and find files where
 * functional tests are found for a given function.
 */