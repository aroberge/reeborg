QUnit.module("Examples from Vincent Maille", {
  beforeEach: function() {
    test_utils.reset();
    test_utils.set_human_language("fr");
  }
});

QUnit.test("Maille", function(assert) {
    "use strict";
    assert.ok(false, "Maille test run")
});